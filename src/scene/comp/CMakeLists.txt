set(DIR src/scene/comp)

set(SOURCES
	${SOURCES}
	${DIR}/animator.cpp
	${DIR}/camera.cpp
	${DIR}/light.cpp
	${DIR}/light_probe.cpp
	${DIR}/mesh_renderer.cpp
	${DIR}/rig.cpp
	${DIR}/script.cpp
	PARENT_SCOPE
)
